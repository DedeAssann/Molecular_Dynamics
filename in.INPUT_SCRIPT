# (1) Defining units boundary conditions and atom_style

units                metal
boundary             p p p
atom_style           charge
variable	stp	equal	100	# definig the step variable

# (2) Specification of potential type and cut off

print "The number of steps I will run is = ${stp}"   # Just a good old print!

region bigbox block	0 100 0 100 0 100 units box

# - 'region' refers to an abstract region of space
# - 'forbox' is just an ID assigned to this region of space, that can be used later to refer to it
# - 'units box' refers to the fact that the size of the box is specified in the units as given
# in the units command

# Creating the simulation box

create_box 1 bigbox

# Set of points in space
lattice custom 1.0 a1 1.0 0.0 0.0 a2 0.0 1.0 0.0 a3 0.0 0.0 1.0 &
		    basis 0.0 0.0 0.0 &
		    basis 0.5 0.5 0.0 &
		    basis 0.0 0.5 0.5 &
		    basis 0.5 0.0 0.5
# Defining the lattice/grid with 4.58 as argument for the scale keyword,
# while a1, a2 and a3 are the tree lattice vectors(les vecteurs de bases de l'espace)
# it is followed by the basis commends giving the location of atoms in one unit cell

timestep	0.001

create_atoms 1 region forbox basis 1 1 basis 2 1 basis 3 1 basis 4 1 units box
# creating the atoms and putting them in the box


